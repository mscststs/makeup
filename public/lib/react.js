export const {
  useState,
  useEffect,
  useContext,
  createContext,
  useReducer,
  useRef,
  useMemo,
  useCallback,
  Fragment,
  Suspense,
  lazy,
} = window.React;
export default window.React.default;