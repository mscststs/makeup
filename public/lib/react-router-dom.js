
export const {
  BrowserRouter,
  HashRouter,
  Link,
  NavLink,
  Route,
  Routes,
  useNavigate,
  useLocation,
  useParams,
  useMatch,
} = window.ReactRouterDom;

export default window.ReactRouterDom.default;